# Wasabi bucket config for Photo Drop (non-secret; access keys live in
# Secret Manager — see README "Photo Drop (Wasabi) setup").
# Region must match where the bucket was created, e.g. us-east-1, us-central-1.
WASABI_BUCKET=mchs-photo-drop
WASABI_REGION=us-east-1
